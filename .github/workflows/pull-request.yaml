name: Pull request
on:
    pull_request:
permissions:
    contents: read
jobs:
    ci:
        name: CI
        runs-on: ubuntu-22.04
        steps:
            - name: Checkout repository
              uses: actions/checkout@v6

            - name: CI
              uses: ./.github/actions/ci
