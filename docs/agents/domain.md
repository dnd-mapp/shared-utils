# Domain Docs

**Layout: single-context.**

- Domain language: `CONTEXT.md` at the repo root (create if absent)
- Architectural decisions: `docs/adr/` at the repo root (create if absent)

Skills that read domain docs (`improve-codebase-architecture`, `diagnose`, `tdd`) should look in these two locations only.
